# Random long string; rotate for production
PAYLOAD_SECRET=change-me
# Dev: sqlite file. Production: Neon Postgres connection string
# (swap adapter in src/payload.config.ts: @payloadcms/db-sqlite -> @payloadcms/db-postgres)
DATABASE_URI=file:./tampa-taruno.db
# Password given to the seeded admin user (change after first login)
SEED_ADMIN_PASSWORD=change-me
